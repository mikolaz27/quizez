from config.settings.base import *  # noqa:

CURRENT_ENV = "PROD"
print(CURRENT_ENV)

DEBUG = False
# SECURITY WARNING: don't run with debug turned on in production!
# TODO update later
ALLOWED_HOSTS = ["*"]
