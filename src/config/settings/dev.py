from config.settings.base import *  # noqa:

DEBUG = True

CURRENT_ENV = "DEV"
print(CURRENT_ENV)
# SECURITY WARNING: don't run with debug turned on in production!
ALLOWED_HOSTS = []

# INSTALLED_APPS += ""
